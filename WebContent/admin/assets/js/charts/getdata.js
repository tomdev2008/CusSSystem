// $(function () {
// var host = window.location.host
// var socket = new WebSocket("ws://" + host + "/testkf/websocket/fwq");
// //接收服务器的消息
// socket.onmessage = function (ev) {
//     var obj = eval('(' + ev.data + ')');
//     addMessage(obj);
// }

// });
